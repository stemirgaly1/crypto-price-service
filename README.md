# Crypto Price Service

Тестовое задание на позицию Junior Backend Developer.

Сервис предназначен для периодического получения цен криптовалют с биржи Deribit и предоставления REST API для работы с сохранёнными данными.

---

## 🚀 Функциональность

* Периодическое (каждую минуту) получение index price для:

  * btc_usd
  * eth_usd
* Сохранение данных в PostgreSQL:

  * тикер валюты
  * цена
  * UNIX timestamp
* REST API на FastAPI для работы с данными

---

## 🧱 Архитектура проекта

app/
├── api/          # HTTP API (FastAPI роуты)
├── clients/      # HTTP клиенты (Deribit)
├── core/         # Конфигурация приложения
├── db/           # Работа с БД (SQLAlchemy)
├── services/     # Бизнес-логика
├── tasks/        # Celery задачи
├── main.py       # Точка входа FastAPI
Архитектура разделена на слои, что упрощает поддержку, тестирование и расширение функциональности.

---

## 🛠️ Стек технологий

* Python 3.12
* FastAPI — HTTP API
* SQLAlchemy (async) — ORM
* PostgreSQL — база данных
* Celery — фоновые задачи
* Redis — брокер сообщений для Celery
* aiohttp — асинхронный HTTP-клиент
* pydantic-settings — конфигурация приложения

---

## ⚙️ Переменные окружения

Создайте файл .env в корне проекта:

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/crypto
> ⚠️ Файл .env не должен попадать в репозиторий

---

## ▶️ Запуск проекта (локально)

### 1️⃣ Создать виртуальное окружение

python -m venv .venv
.venv\Scripts\activate
### 2️⃣ Установить зависимости

pip install -r requirements.txt
(или установить вручную)

---

### 3️⃣ Запуск PostgreSQL

PostgreSQL должен быть запущен и содержать БД crypto.

---

### 4️⃣ Запуск FastAPI

uvicorn app.main:app --reload
API будет доступно по адресу:

http://127.0.0.1:8000
Swagger:

http://127.0.0.1:8000/docs
---

### 5️⃣ Запуск Redis

redis-server
---

### 6️⃣ Запуск Celery worker

celery -A app.tasks.celery_app worker -l info
---

## 📡 API эндпоинты

Все методы используют GET и обязательный query-параметр ticker.

### 🔹 Получить все данные по валюте

GET /prices/all?ticker=btc_usd
---

### 🔹 Получить последнюю цену

GET /prices/latest?ticker=btc_usd
---

### 🔹 Получить цены за период

GET /prices/by-date?ticker=btc_usd&date_from=1700000000&date_to=1700003600
---

## 🧠 Design Decisions

### 1. Разделение на слои

Проект разделён на API, Service и DB слои:

* API слой не содержит SQL
* Бизнес-логика вынесена в сервисы
* Работа с БД изолирована

Это повышает читаемость, тестируемость и масштабируемость.

---

### 2. Асинхронный стек

Использованы async версии:

* FastAPI
* SQLAlchemy (async)
* asyncpg
* aiohttp

Это позволяет эффективно работать с I/O операциями.

---

### 3. Celery для фоновых задач

Celery используется для периодического получения цен:

* не блокирует API
* легко масштабируется
* подходит для production

---

### 4. Конфигурация через .env

Чувствительные данные вынесены из кода:

* безопасно
* удобно для разных окружений

---



## 👨‍💻 Автор

Тестовое задание выполнено в рамках отбора на позицию Junior Backend Developer.